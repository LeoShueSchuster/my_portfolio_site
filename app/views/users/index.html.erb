<header id="content-header">
  <h3><%= pluralize(@users.size, 'User') %></h3>
</header>

<ul id="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>